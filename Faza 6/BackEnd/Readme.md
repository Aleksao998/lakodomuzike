Modules need to install:
    npm init
    npm install express
    npm install dotenv
    npm install -D nodemon

    npm install colors

    // for logger
    npm install morgan

    // for db
    npm install mongoose

    

Server running on port: 5000;